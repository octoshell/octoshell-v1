en:
  call_callbacks: Call Callbacks
  helpers:
    submit:
      create: Create
      update: Update
      project_joiner:
        create: Use
      user:
        create: Sign Up
  markdown_helpers:
    edit: Edit
    preview: Preview
    syntax: Flavored with <a href="http://daringfireball.net/projects/markdown/syntax">Markdown</a> syntax
  activerecord:
    attributes:
      project:
        organization: Main Organization
    errors:
      models:
        session:
          attributes:
            base:
              date_is_out_of_available_range: Impossible to create a session
  activemodel:
    errors:
      models:
        project_joiner:
          attributes:
            code:
              not_found: not found
