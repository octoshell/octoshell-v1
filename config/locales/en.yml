en:
  call_callbacks: Call Callbacks
  helpers:
    submit:
      create: Create
      update: Update
      project_joiner:
        create: Use
      user:
        create: Sign Up
  activerecord:
    attributes:
      project:
        organization: Main Organization
  activemodel:
    errors:
      models:
        project_joiner:
          attributes:
            code:
              not_found: not found
