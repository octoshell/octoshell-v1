li class="#{active_menu?(:dashboard)}" = link_to 'Рабочий кабинет', dashboard_path
- if admin?
  li class="#{active_menu?(:admin)}" = link_to 'Кабинет Администратора', admin_path
li class="#{active_menu?(:support)}" = link_to 'Поддержка', tickets_path
li class="#{active_menu?(:notifications)}" = link_to "Оповещения (#{current_user.notifications_count})", notifications_path
