- title 'Организации'

div class="link-actions"
  = link_to 'Новая организация', new_organization_path, class: 'btn'

= simple_form_for @search, url: organizations_path, method: :get, html: { class: 'form-horizontal' } do |f|
  = autocomplete :organization, f, name: :id_eq, label: "Название"
  = f.input :abbreviation_contains, required: false
  = f.input :organization_kind_id_eq, collection: OrganizationKind.all, required: false
  = f.input :state_in, collection: Organization.human_state_names, input_html: { multiple: true }, required: false
  div class="form-actions"
    = f.button :submit, 'Найти', class: 'btn-primary'

= safe_paginate @organizations
= render partial: 'table', locals: { organizations: @organizations }
= safe_paginate @organizations
