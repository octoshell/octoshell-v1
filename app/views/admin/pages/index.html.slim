- title 'Справка'

div class="link-actions"
  = link_to 'Новая страница', new_page_path, class: 'btn'

- if @pages.any?
  ul
    - @pages.each do |page|
      li
        = link_to page.name, page
        span class="actions"
          | 
          small = link_to 'редактировать', edit_page_path(page.id)
          |  / 
          small = link_to 'удалить', page_path(page.id), method: :delete, confirm: 'Вы уверены?', class: 'danger'
          | 
- else
  p Нет страниц