- if memberships.any?
  ul class="unstyled"
    - memberships.each do |membership|
      li
        = link_to_admin_organization membership.organization
        |  - 
        small = membership.positions.map { |p| "#{p.name}: #{p.value}" }.join(', ')
- else
  p Нет мест работы