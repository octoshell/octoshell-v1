- if can? :index, :projects
  = submenu_item :projects, (link_to t('.projects'), projects_path)
- if can? :index, :requests
  = submenu_item :requests, (link_to t('.requests'), requests_path)
- if can? :index, :clusters
  = submenu_item :clusters, (link_to t('.clusters'), clusters_path)
- if can? :show, :profiles
  = submenu_item :profiles, (link_to t('.profile'), profile_path)
- if can? :show, :tickets
  = submenu_item :ticketsm, (link_to t('.tickets'), tickets_path)