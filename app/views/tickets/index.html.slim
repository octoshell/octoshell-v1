- title 'Заявки'

div class="link-actions"
  = link_to 'Новая заявка', new_ticket_path, class: 'btn'
  = " "
  = link_to 'Закрытые заявки', closed_tickets_path, class: 'btn'

h2 Активные
= render partial: 'table', locals: { tickets: @tickets.active }

h2 Отвеченные
= render partial: 'table', locals: { tickets: @tickets.answered }

h2 Решенные
= render partial: 'table', locals: { tickets: @tickets.resolved }