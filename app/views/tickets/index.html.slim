- title t('.title', default: 'Tickets')

div class="link-actions"
  = link_to t('.new_title', default: 'Новая заявка'), new_ticket_path, class: 'btn'

= safe_paginate @tickets
= render partial: 'table', locals: { tickets: @tickets }
= safe_paginate @tickets
