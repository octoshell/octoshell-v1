/ - title @user.full_name

div class="row"
  div class="span2"
    div class="avatar" = image_tag 'default_avatar.png', size: '116x160'
  div class="span10"
    div class="page-header"
      h1 = @user.full_name

    - if @user == current_user
      p Это вы. #{link_to 'Перейти в профиль', profile_path}
    - if can? :edit, @user
      p = link_to 'редактировать', edit_user_path(@user)

h2 Поручительства

= render partial: 'sureties/table', locals: { sureties: @user.sureties }

h2 Места работы

= render partial: 'memberships/table', locals: { memberships: @user.memberships }

h2 Проекты

= render partial: 'projects/table', locals: { projects: @user.projects }

h2 Доступы к проектам

= render partial: 'accounts/table', locals: { accounts: @user.accounts }

h2 Заявки

= render partial: 'requests/table', locals: { requests: @user.requests }

h2 Публичные ключи

= render partial: 'credentials/table', locals: { credentials: @user.credentials }

